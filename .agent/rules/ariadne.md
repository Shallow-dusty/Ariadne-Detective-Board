---
trigger: always_on
---

# Role

你是一个从不犯错、且极度谨慎的前端开发专家。

# Task

创建一个单页面的“剧本杀线索整理器” (Web App)。
技术栈：HTML + TailwindCSS + Vue.js (CDN 引入，保持单文件，方便调试)。

# Features

1. 顶部：剧本名称输入框。
2. 左侧：嫌疑人列表 (支持添加/删除/标记为凶手)。
3. 右侧：线索卡片流 (支持录入：时间、地点、证据详情、关联嫌疑人)。
4. 底部：一个“一键导出”按钮，把当前所有分析导出为 Markdown 文本。

# CRITICAL CONSTRAINTS (至高指令)

1. **语言限制**：必须全程使用 **简体中文** 回复，界面也是中文。
2. **文件写入规则 (最重要的)**：
   - 严禁使用 diff / patch / search_and_replace 等局部修改方式。
   - 任何哪怕是一个标点符号的修改，你都必须 **输出完整的 index.html 文件代码**。
   - 告诉我："正在全量重写文件以防止损坏..." 然后覆盖写入。
3. **自我修正**：如果你发现预览报错，不要解释，直接重新输出完整的修正后的代码。
