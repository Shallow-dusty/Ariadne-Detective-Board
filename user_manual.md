# Ariadne - 使用手册 (User Manual)

## 欢迎使用 Ariadne

Ariadne (阿里阿德涅) 是您的私人侦探助手。它利用现代化的 Web 技术，帮助您在剧本杀、跑团或真实案件推演中整理错综复杂的线索。

---

## 🖥️ 桌面端指南

桌面端采用经典的**三栏布局**，让您一眼掌控全局。

### 1. 左侧：嫌疑人名册 (Suspects Dossiers)

这里是所有登场人物的档案库。

- **添加嫌疑人**：点击标题栏右侧的 `+` 按钮。
- **编辑信息**：
  - **改名**：直接点击名字输入框修改。
  - **备注**：点击下方的备注栏，记录动机或疑点。
- **标记凶手**：点击卡片底部的 `💀 标记凶手` 按钮。被标记的人物头像会变红，并在关联图中显示为红色节点。
- **删除**：点击卡片底部的 `🗑️` 按钮。

### 2. 中间：线索流 (Clue Stream)

这是案情推演的核心，按录入顺序倒序排列。

- **录入线索**：
  - **时间**：输入线索发生的时间（如 `19:30`）。
  - **内容**：输入线索详情。
    - **关联人物**：在文字中输入 `@` 加上人名（如 `@管家`），系统会自动高亮并关联该嫌疑人。
    - **添加图片**：点击输入框右侧的 `🖼️` 图标上传图片，或直接 **Ctrl+V** 粘贴截图。
  - **提交**：按回车或点击 `+` 按钮。
- **查看大图**：点击线索卡片中的图片，可进入大图预览模式（Lightbox）。

### 3. 右侧：关联图谱 (Investigation Board)

这是您的“神探大脑”，系统会自动绘制人物关系。

- **自动连线**：如果您的一条线索关联了 **2 个或以上** 的嫌疑人（例如：“@死者 和 @女仆 发生了争吵”），关联图中这两个人物节点之间就会自动连上一条**红线**。
- **节点状态**：
  - **蓝色/黑色节点**：普通嫌疑人。
  - **红色节点**：被标记为“凶手”的嫌疑人。

### 4. 顶部功能栏

- **剧本标题**：点击顶部中间的标题即可修改。
- **重置**：点击左侧的 `↺` 按钮清空当前所有数据（慎用！）。
- **导入/导出**：
  - `📥 导入数据`：加载之前保存的 JSON 剧本文件。
  - `📤 备份数据` (JSON)：将当前状态完整保存为文件，方便发给队友或换电脑使用。
  - `📋 导出报告` (Markdown)：生成一份精美的文字版案情报告，适合复盘分享。

---

## 📱 移动端指南 (App 模式)

在手机上访问时，Ariadne 会自动变身为原生 App 体验。

### 底部导航栏

由于手机屏幕限制，我们将三栏布局拆分为三个独立的视图，通过底部的标签栏切换：

1. **👥 嫌疑人**：管理人物档案。
2. **📄 线索墙**：录入和查看线索流（默认首页）。
   - _提示_：如果有新线索，图标上会有红点提示。
3. **🕸️ 关联图**：查看人物关系网。

### 移动端特色操作

- **图片预览**：点击线索图片，支持全屏缩放查看。
- **快速切换**：在推演过程中，您可以随时在“线索墙”和“关联图”之间切换，边看证据边理思路。

---

## 常见问题 (FAQ)

**Q: 关闭浏览器数据会丢吗？**
A: **不会**。Ariadne 使用浏览器本地存储 (LocalStorage)，刷新或关闭页面后再次打开，数据依然都在。但为了安全起见（如清理浏览器缓存），建议定期点击 `📤 备份数据`。

**Q: 怎么在两个设备间同步？**
A: 目前没有云端同步。请在一台设备上点击 `备份数据` 导出 JSON 文件，发送给另一台设备，然后点击 `导入数据` 即可。

**Q: 关联图为什么是空的？**
A: 您需要先添加嫌疑人，才会有节点。只有当一条线索同时关联了多个人物时，才会出现连线。
